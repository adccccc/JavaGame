package main.system.collision.shape;

public class Circle {

    int x, y; // 圆心坐标(相对)
    int radius; // 半径

    public Circle(int x, int y, int radius) {
        this.x = x;
        this.y = y;
        this.radius = radius;
    }
}